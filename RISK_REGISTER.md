# Production Risk Register

Дата актуализации: 2026-09-06. Статусы: `RESOLVED`, `PARTIAL`, `OPEN`, `NOT VERIFIED`. Сокращённые
`core/...`, `web/...` и `cli/...` пути в таблице имеют общий префикс
`src/yt_uniquifier/`.

`RESOLVED` означает наличие regression test и локально пройденного соответствующего
gate. Это не переносит результат автоматически на непроверенные OS/GPU/HDR cases.

| Priority | Component | Problem | Evidence | Impact | Recommendation |
|---|---|---|---|---|---|
| P1 | VFR keyframe origin on FFmpeg 9 | **CONFIRMED, OPEN:** a generated 3.703 s MKV reports initial stream start 0, but `-skip_frame nokey -show_frames -show_streams` reports start 2.803 s after the scan; real keyframes 0/0.834/1.668/2.503/3.337 become 0/0.534 in the planner | `core/segmenter.py::list_keyframes`; VFR fixture in `tests/integration/test_qa_bounded_resources.py` | Normalization can shift keyframe boundaries or collapse segmentation; the reference-storage test injects known valid VFR boundaries and does not qualify this planner behavior | Read origin before frame scanning in an independent probe; invalidate keyframe-cache schema and test offset-origin plus VFR on FFmpeg 5/6/9. Keep separate from the already launched v6 baseline |
| P1 | Concurrent resource registry | **RESOLVED: six-cell CI 34037614644 including both Windows versions passed:** completed reservation disappeared between glob/stat, and transient sharing locks prevented atomic resize/release | `core/resource_budget.py::_active_reserved_bytes`, `DiskReservation.resize`, `core/output_reservation.py::_release_exact_owner`; two regressions failed before the fix; CI `34036608149` Windows 3.12 frontend-concurrency failure | Healthy concurrent CLI/web/worker jobs can fail despite available resources | Skip genuinely disappeared records; bounded permission retries reuse the cache retry policy; recheck owner on each release attempt; malformed/foreign records remain fail-closed |
| P1 | Fractional pitch clock | **RESOLVED in focused FFmpeg 5/6/9 tests and six-cell CI 34037614644:** compensation used nominal pitch instead of integer `asetrate`, introducing 8 ppm (~86 ms/3 h) at 44100 Hz / 1.0004 | `core/transforms/audio_pitch.py::_build_pitch_tempo`, `_tempo_literal`; three initially failing clock tests and `tests/integration/test_pitch_clock_long_event.py` | Internal audio drift survives final sample-count pad/trim; completed legacy natural baseline measured 0/-50/-100 ms at start/middle/end | Compensate actual integer rate, retain precision and omit unity WSOLA; encode-policy v6 prevents reuse of pre-fix artifacts. A full three-hour v6 rerender and human lip-sync review remain unverified |
| P3 | Long-form similarity diagnostics RAM | **FIXED in focused tests; full-run RSS pending:** legacy QA reached 6,171,004 KiB for 10,820 frames/file; long-form extraction now streams one PNG into a compact hash, and the image cache has a 64 MiB estimated payload budget | `core/qa/cid_predict.py::predict`; `core/qa/phash.py::_sample_hashes`, `_cache_frames`; streamed/legacy real-grid equivalence and decoder-failure cleanup tests | Removes movie-length image retention without changing sampling; 64 MiB is not a hard whole-process/FFmpeg RSS limit | Complete `long-v6-bounded-qa` before claiming a measured three-hour RAM reduction; keep OS/container limits for deployment |
| P0 | Main audio origin | **RESOLVED for qualified paths:** PTS reset discarded leading audio silence | `core/pipeline.py::_main_audio_input`; real FFmpeg 5/6/9 delayed-pulse tests; CI `34030418205`; natural lag -1.32 → -0.01 s at 10-ms resolution | Internal A/V desync despite matching output duration/sample count | Silence before tempo/PTS reset and loudness measurement; normalize once before window split; invalidate old artifacts. Natural editorial lip-sync still needs human review |
| P1 | Windowed audio speed/headroom | **RESOLVED in focused tests:** fixed input-clock crossfade shortened speed-adjusted windows; short fallback discarded peak-retry gain | `core/pipeline.py::build_main_audio_command_windowed`; windowed unit tests and real delayed pulses at 1x/2x | Accumulated event shift or repeated peak-gate failures | Scale crossfade by output/source duration and forward post-gain into the fallback |
| P2 | Low VMAF semantics | **RESOLVED locally under accepted RFC #21:** retain valid zero/near-zero scores; explicit requested metrics must independently pass | `core/qa/vmaf.py::compute`, `core/qa/report.py::_policy_quality`; `tests/unit/test_qa_evidence_policy.py` | Low quality can no longer disappear behind SSIM substitution in explicit VMAF gates | Numeric validity and scoring-domain validity are separate; preserved HDR remains unsupported for VMAF gating, and natural quality thresholds still require calibration |
| P2 | Natural audio alignment confidence | **OPEN / diagnostic only:** the baseline fingerprint report did not expose the reproduced 1.32-second audio advance | `core/qa/audio_fp.py::align_fingerprints`; `extended-4k-surround` baseline reported offset 0 with confidence about 0.535 | A similarity/alignment field can appear reassuring while audible events are misplaced | Keep confidence/coverage visible, retain independent event/envelope checks, and calibrate natural-content gates under accepted QA contract RFC #21; do not infer internal sync from endpoints |
| P1 | Release SBOM scope | **PARTIAL, new workflow qualified:** per-artifact inventories cover actual native bundle/AppImage files and shipped Python metadata; original Linux environment SBOM retained separately | `tools/bundle_sbom.py`; Release `34037678414` inventories schema/archive-hash-checked locally, all 10 checksums and 11 signatures passed; archive/commit binding regressions | File integrity is covered but opaque PYZ/static dependencies, external OS libraries and full license attribution remain unverified | Retain explicit inventory scope and do not call file hashes a complete dependency graph; each immutable tagged release still needs its own checks |
| P2 | Registered long-form quality | **PARTIAL:** legacy two-copy estimate exceeded the disk budget; unchanged-duration reference generation now falls back to a single-copy virtual FFV1 concat when it fits, with live disk/reserve checks | `core/qa/registration.py::build_transformed_reference`; physical/virtual pixel, PTS and metric regressions; `long-v6-bounded-qa` pending | Full coverage is retained; one reference still needs disk space, and insufficient budgets fail closed. Short VFR storage tests inject valid boundaries, not planner qualification | Finish full v6 registered metrics and human acceptance; retimed references retain the existing physical-concat guard; do not treat missing metrics as quality passes |
| P1 | Windows audit paths | **RESOLVED: Windows CI 34034216736 passed:** home-prefix redaction ran before full absolute-path redaction | `core/redaction.py::redact_path`, `redact_mapping`; Windows CI `34028444412` and `tests/unit/test_redaction_home_paths.py` | Web audit retained private subdirectories under `<HOME>`; space-containing fields could also be partially exposed | Redact complete absolute fields before home-only/token processing; cross-OS path tests and unchanged private home-only contract |
| P2 | Untagged surround input | **OPEN:** channel count alone does not identify speakers | `core/pipeline.py::_main_audio_channel_args`; six-channel PCM/MKV on FFmpeg 5 has no probed speaker mask | AAC negotiation retains six channels using FFmpeg's existing default layout assumption, but source speaker placement cannot be certified | Supply correctly tagged surround sources; qualify channel identity with speaker-specific markers, not only channel counts |
| P2 | Source-derived VBV cap | **CONFIRMED / default unchanged:** paired 18-encode experiment found about 14–15 VMAF points of encoding loss attributable to the source-derived cap on PQ/HLG→SDR cases | `core/pipeline.py::_max_bitrate_for`; `tools/rate_control_experiment.py`; `BENCHMARKS.md` | Compressed source bitrate does not budget additional noise/transforms; removing cap increased size about 11–13× | Design an explicit bounded quality/size policy using broader content and human labels; do not remove all caps or infer universal VMAF thresholds from three related clips |
| P0 | Audio pitch | **RESOLVED:** 44.1 kHz source обрабатывался как 48 kHz | `core/pipeline.py::_audio_transform_params`; real FFmpeg 44.1 kHz test | Massive A/V desync, потеря хвоста | Source-aware `asetrate`; final 48 kHz; duration/LUFS regression |
| P0 | Segment timestamps | **RESOLVED:** copied negative AAC PTS сдвигал encoded video | `core/pipeline.py::build_segment_command_fused`; 752/752 decoded frames | Потеря начала/хвоста, A/V offset | Video-only segment, `setpts=PTS-STARTPTS`, final contract |
| P0 | Main audio mapping | **RESOLVED:** профиль без audio transforms терял track 0 | `core/segmenter.py::concat_segments`; real mux regression | Output без audio | Explicit source main-audio mapping |
| P0 | Chapters | **RESOLVED:** source chapters не передавались в concat | `core/orchestrator.py::_run_full_body`; 2→2 chapter test | Потеря навигации/метаданных | Source chapter mapping + title metadata restoration |
| P0 | Subtitle mux | **RESOLVED:** subtitle codec игнорировал target container | `core/preflight.py::_check_subtitles`; `core/segmenter.py::concat_segments` | Late mux failure | MP4/MOV text→mov_text; image subtitle early reject; MKV copy |
| P0 | Windowed audio | **RESOLVED for synthetic contract:** physical overlap был вдвое больше crossfade | `core/pipeline.py::build_main_audio_command_windowed`; real 125 s duration and 180 s six-boundary-event tests | Accumulated shift, repeated/dropped boundary content, tail cut | Half-overlap per side; one-AAC-frame duration gate and exact event-count/30 ms timing gate; natural listening remains open |
| P0 | SSCD calibration | **RESOLVED:** objective был инвертирован | `core/calibration/loop.py::_evaluate_sscd`; direction tests | Идентичный output считался успехом | Direct clamped similarity |
| P0 | Resume identity | **RESOLVED:** plan hash не удостоверял content/topology | `core/pipeline.py::compute_plan_hash`; replacement/topology tests | Foreign stale artifacts | Content fingerprint + complete topology digest |
| P1 | Loudnorm | **RESOLVED:** pass 1 измерял source до preceding transforms, а actual pass-2 mode не наблюдался | `core/pipeline.py::_measure_before_loudnorm`; `audio_loudnorm.py::normalization_mode_for`; `segmenter.py::_record_loudnorm_mode`; real -14 LUFS test | Ошибочное предположение о linear mode и silent dynamic fallback | Measure actual pre-loudnorm graph; parse only a bounded log tail; record requested/reported mode and distinct fallback reason in log/event |
| P1 | Audio sample rate | **RESOLVED:** dynamic loudnorm output SR был implicit | `core/pipeline.py`; real output 48 kHz | Неожиданный format/size | Explicit final 48 kHz |
| P1 | Delivered audio peak | **RESOLVED for tested fixtures: six-cell CI 34034216736 passed:** pre-encode loudnorm -1.50 dBTP became -1.05 in resampled float PCM and +0.27 after AAC on natural 5.1 | `core/pipeline.py::_main_audio_tail_filter`, `core/segmenter.py::process_main_audio`, `require_audio_delivery_peak`; `tests/integration/test_audio_delivery_peak.py`; corrected natural full stream -3.38 dBTP | Codec/resampling overshoot can clip despite a nominal loudness target | Measure encoded audio; at most three source renders with linked headroom, then fail closed. Cached/main/final audio use the same gate. Peak safety can lower LUFS; human listening remains required. Original +1.40 dBTP excerpt used input seeking and was not a full-file measurement |
| P1 | Long-form audio tail | **RESOLVED locally:** stateful pitch/tempo + dynamic loudnorm dropped 2848 normalized samples on a 95-minute source and increased A/V end gap to 146 ms | `core/pipeline.py::_main_audio_tail_filter`; 44.1 kHz real-FFmpeg regression plus 274426152-sample full natural-film audio encode/remux/decode | Audible missing tail or visible end desync despite acceptable container duration | Resample to 48 kHz, pad/trim exact target sample count and reset PTS; verified local end delta 4.5 ms, retain native Linux/Windows long-form qualification |
| P1 | Profile contract | **RESOLVED:** `target_loudness_lufs` игнорировался | `core/pipeline.py::_loudnorm_params_from`; command regression | Неверная loudness | Top-level target unless transform explicitly overrides |
| P1 | Final cache | **RESOLVED:** no-op проверял только non-empty output | `core/checkpoint.py::output_is_valid`; orchestrator | Corrupt/wrong output accepted | Exact output path/hash plus final media contract |
| P1 | Cached main audio | **RESOLVED:** cached audio не имел integrity check | `core/checkpoint.py`; `segmenter.py::process_main_audio` | Corrupt audio reused | SHA256 + atomic temp replace |
| P1 | Speed/duration | **RESOLVED for qualified combinations; six-cell CI 34034216736 passed:** matching main A/V, additional audio, SRT and chapters retimed | `core/subtitle_timing.py`, `core/segmenter.py::concat_segments`, `core/preflight.py::_check_chapter_container`; 16 media tests on FFmpeg 5/6 plus three real chapter-gap cases, 19 native FFmpeg 9 cases | Earlier CI exposed ~21 ms subtitle / ~23 ms chapter shifts; FFmpeg 5 also discarded subtitle payload with packet-BSF retiming | Canonical SRT headers, explicit chapter metadata and disabled global priming shifts. Reject leading chapter gaps for MP4/MOV; MKV preserves them. ASS/bitmap/timed-data retiming remains fail-closed |
| P1 | SAR/DAR | **RESOLVED:** crop/resize оставлял неверный SAR | `core/transforms/video_geom.py`; real SAR 1:1 test | Искажённое отображение | `setsar=1` after scale |
| P1 | Stream topology | **RESOLVED for declared policy:** selected A/V/S metadata, real PGS-in-MKV, MKV attachments, MOV tmcd и MP4 JPEG/PNG cover art preserved/validated; incompatible PGS/auxiliary and multi-program-video inputs rejected | `core/auxiliary_streams.py`, `core/probe.py`, `core/segmenter.py`, `core/media_validation.py`; byte/timecode/PGS integration tests | Silent stream loss prevented | Keep uncommon data codecs NOT VERIFIED until licensed fixtures exist |
| P1 | Profile audio_tracks | **RESOLVED:** extras игнорировали profile selection | `core/stream_policy.py`; orchestrator/concat tests | Contract mismatch/privacy/size surprise | Unified first/all/absolute-index selection |
| P1 | Metadata | **RESOLVED for supported containers:** selected stream/chapter metadata plus attachment filename/mimetype and timecode are preserved/validated | `core/metadata.py`, `core/auxiliary_streams.py`, `core/media_validation.py` | Unsupported metadata no longer disappears silently | Extend only from real muxer-qualified fixtures |
| P1 | HDR metadata | **PARTIAL:** static ST2086/CLL+x265 and HLG+x265/VideoToolbox preservation verified; dynamic HDR rejected; hardware static metadata remains open | `core/probe.py`, `core/pipeline.py`, `core/preflight.py`; real HDR/HLG tests and Intel Mac smoke | Silent HDR loss предотвращён, но vendor/natural matrix неполна | Квалифицировать natural HDR corpus и static metadata на каждом advertised hardware encoder |
| P1 | HDR output policy | **RESOLVED:** HDR with neither `keep_hdr` nor a first-video-stage `video.tonemap_sdr` reached an 8-bit encode and failed only at final validation | `core/preflight.py::_check_hdr`, `_check_tonemap_order`; unit and real HDR regressions | Full encode wasted; PQ/HLG math could run in the wrong domain when enforcement was bypassed | Default production path fails before segment creation; unsafe override remains explicit through `--no-preflight` |
| P1 | HDR colour math | **RESOLVED locally:** transfer-only linearisation in subsampled YUV caused severe green/orange highlights although metadata remained valid | `core/transforms/hdr_wrap.py::wrap_linear`; independent FFV1/HEVC regression; checksum-pinned natural-scene A/B review | Visibly corrupted preserved PQ/HLG output passed metadata-only tests | Use explicit `gbrpf32le` linear-light working space and explicit BT.2020/10-bit return; retain native-camera HDR corpus qualification |
| P1 | Encoder capability | **RESOLVED locally:** discovery дополнен exact job resolution/pixfmt/color/RC probe; cache включает OS/GPU signature | `core/encoder.py::probe_encoder_for_plan`, `core/pipeline.py::build_encoder_capability_probe` | Unsupported job fails before segments | Сохранять per-platform qualification artifacts; invalidate on runtime device failure |
| P1 | Runner hang | **RESOLVED:** bounded streaming log, 600 s silent-stall watchdog, optional wall policy, process-group termination | `core/runner.py`; real silent/heartbeat/wall tests | Hung job terminates with diagnosis | Оставить wall timeout opt-in для long-form; monitor timeout events |
| P1 | Checkpoint lock | **RESOLVED locally:** check-then-replace was not exclusive | `core/checkpoint.py::CheckpointStore`; concurrent lock tests | Concurrent writers | Atomic `O_CREAT|O_EXCL`; conservative cross-host stale policy |
| P1 | Lock lifecycle | **RESOLVED:** initialization failures leaked the lock and `--new-variant` archived state before ownership | `core/orchestrator.py::_run_full_impl`; lifecycle regressions | Daemon blocked another process; rejected concurrent run could mutate active state | Acquire before archive; unconditional close from initialization onward |
| P1 | Atomic persistence/publication | **RESOLVED locally:** injected checkpoint `fsync` and final output replace failures left temporary artifacts; full process-group SIGKILL now resumes after probe, plan, segment, during audio/concat/validation and after publication before final validation | `core/checkpoint.py::_flush`; `core/segmenter.py::concat_segments`; `tests/integration/test_pipeline_phase_sigkill.py`; 7/7 local phase faults | Disk leak/confusing recovery while previous valid artifact must survive | Unique temporary paths, cleanup without masking original persistence error, explicit publication error; retain production NFS/power-loss qualification |
| P1 | Parallel failure | **RESOLVED:** siblings cancelled only with caller token | `core/segmenter.py::process_video_segments_parallel` | Slow failure | Always-present internal cancellation token |
| P1 | Resource scheduling | **PARTIAL:** web run count plus encoder slots and workspace/final byte reservations are shared by local processes; A/V estimates include every audio stream, while workspace and projected-final reservations atomically resize from measured completed artifacts. Reference Compose adds hard CPU/RAM/PID ceilings | `core/resource_budget.py::DiskReservation.resize`; `core/orchestrator.py::_refresh_workspace_disk_budget`; `docker-compose.yml`; atomic grow/shrink/owner/replace regressions | Common local oversubscription and stale over-reservation are bounded, but split registry paths/UIDs, native processes outside OS quotas or mixed GPU visibility can still exhaust resources | Keep deployment-wide registry and hard OS/container quotas; add per-device routing only with hardware evidence; qualify production NFS semantics |
| P1 | Web output race | **RESOLVED on one shared local filesystem:** duplicate output is protected by an atomic owner record across app processes; dead same-host owners are reclaimed conservatively | `core/output_reservation.py`; `web/routes/run.py::start_run`; two-app and real-subprocess tests | Last writer wins | Keep foreign-host owners fail-closed; qualify `O_EXCL`/recovery semantics on every production NFS/shared-FS deployment |
| P1 | Web lifecycle | **RESOLVED process-locally:** atomic bounded JSON store, TTL/count pruning, interrupted-run recovery, redacted persisted errors | `web/app.py`, `web/routes/run.py`; restart/TTL tests | Status survives restart without unbounded RAM | Multi-instance status aggregation/coordination remains a separate deployment concern |
| P1 | Observability disclosure | **RESOLVED:** nested credentials, absolute media paths and arbitrary metric labels could reach public sinks | `core/redaction.py`, `core/logging_config.py`, `core/audit.py`, `web/audit.py`, `web/metrics.py`; recursive redaction and exposition regressions | Secret/path disclosure and Prometheus cardinality growth | Shared bounded redactor before every sink; fixed state/encoder label vocabularies; correlation IDs remain only in structured logs/events |
| P1 | Web request body limit | **RESOLVED:** body-bearing requests without one unambiguous bounded Content-Length bypassed the documented hard ceiling | `web/middleware/upload_limit.py`; direct ASGI missing/malformed/negative/duplicate/conflicting/oversize tests | Chunked, ambiguous or malformed requests could evade the declared resource boundary | Fail closed with 411/400/413 before route handling; duplicate lengths and transfer-encoding conflicts are rejected; chunked bodies intentionally unsupported |
| P1 | Distributed liveness | **PARTIAL:** process-unique heartbeat, staged output, journal-specific token fence и durable commit journal работают в CLI/GUI; corrected two-client NFSv4 lease/partition/crash lab passed on GitHub-hosted Ubuntu | `core/queue/leasing.py::commit_output/recover_commits`; `tools/docker_nfs_fault_smoke.sh`; `tools/nfs_queue_qualification.py`; Actions run `33881832592`; fault and `os._exit` tests | Local/APFS hard-crash and ephemeral NFS application paths закрыты; production cross-host hard-mount semantics ещё не доказаны | Qualify the actual production NFS hard mount/server/network, foreign-host resume and partitions |
| P1 | Distributed resume | **PARTIAL:** worker использует content/plan-stable work dir и container-correct staged output; unfenced artifacts discarded, fenced publication recoverable; общий `run_full` требует полного A/V decode до commit | `cli/cmd_worker.py::_process_one`; `core/orchestrator.py::_run_full_impl`; `core/queue/leasing.py` | Local hard-crash publication и correctness gate доказаны, но NFS semantics remain open | Cross-host chaos matrix на production mount; rich QA sidecars подключать только как diagnostic policy |
| P1 | Resume topology | **RESOLVED:** одинаковый plan hash принимал старые boundaries после смены `--segment-sec` | `core/checkpoint.py::_segment_topology_matches`; changed-topology regression | Несовместимые `seg_NNNN.mkv` могли попасть в новый concat | Exact boundary comparison invalidates and archives stale state |
| P1 | VFR cadence | **PARTIAL:** average FPS parsing and explicit timestamp passthrough verified for segmented libx264 plus H.264/HEVC VideoToolbox | `core/probe.py::_video_fps`; `core/pipeline.py::_video_cadence_args`; 220-frame real VFR tests; hardware run `33966394736` | Auto-CFR мог дублировать/drop frames, diagnostics показывали nominal 60 instead of 36.67 | Software/Intel VideoToolbox resolved; qualify NVENC/QSV/AMF separately |
| P1 | Non-zero video start PTS | **RESOLVED:** absolute keyframe PTS сравнивались с relative duration; 5 s input давал 9 s segment plan | `core/segmenter.py::list_keyframes`; real MP4 `start_time=5` regression | Лишний encode, out-of-range segments, duplicate/drop risk в legacy stream-copy path | Normalize by video stream start; clamp preroll; invalidate absolute-PTS cache via schema v2 |
| P1 | CFR/concat seam integrity | **RESOLVED for software fixture matrix:** frame count/PTS and sparse long-GOP content checked at every seam | `tests/integration/test_vfr_segmentation.py`; CFR 23.976–60, dynamic 3 s GOP and flash/impulse fixtures | Hidden frame duplicate/drop or internal A/V drift | Keep hardware and natural-content corpus explicitly NOT VERIFIED |
| P2 | QA verdict | **RESOLVED for declared contracts under accepted RFC #21:** correctness failures produce `INVALID`; additive nullable JSON/HTML correctness/loudness evidence preserves legacy fields/defaults | `core/qa/report.py::verdict`, `build_report`; real missing-audio and QA evidence regressions; six-cell CI `34034216736` | Explicit evidence and independent quality gates prevent covered failures being hidden by a diagnostic similarity score | Internal sync and human quality acceptance remain separate; missing measurements are not passing evidence |
| P2 | Duration QA | **RESOLVED for synthetic MP4/MOV/MKV contracts:** duration + first video PTS + complete A/V decode are mandatory; integration gates compare exact decoded frames, normalized audio samples and packet ends | `core/media_validation.py`; `core/orchestrator.py::_run_full_impl`; `tests/integration/test_production_phase1_media_contract.py` | A loudnorm-created PTS gap previously let final `-t` truncate valid AAC tail samples | Rebase transformed audio PTS with `asetpts=N/SR/TB`; retain natural long-form sample/end distributions as a release qualification |
| P2 | Metric fallback | **PARTIAL:** low VMAF substitution and pHash-as-quality are fixed; calibration pins one backend per search, but the calibrate CLI retains one numeric quality threshold; QA CLI has independent gates under RFC #21 | `core/qa/quality.py::quality_score`; `core/calibration/loop.py::calibrate`; backend-change regression | Incomparable scores cannot mix within one run; calibration operators must still choose a backend-appropriate threshold | Do not conflate calibration-search targets with the additive QA per-metric acceptance policy |
| P3 | Audio QA process/coverage | **RESOLVED:** report ran six `fpcalc` calls and covered only the first 600 s | `core/qa/audio_fp.py::analyze_pair`; 620 s Mac smoke | Slow QA and blind long-form tail | One extraction/file; five 120 s start/middle/tail windows for long media |
| P2 | VMAF registration | **RESOLVED for deterministic synthetic contracts (RFC #12):** main reports raw and plan-transformed VMAF/SSIM/SSCD separately, resets local PTS and records bounded coverage/confidence | `core/qa/registration.py::build_transformed_reference`; `core/qa/report.py::build_report`; real crop/mirror/speed/frame-drop matrix | Intentional transforms no longer have to masquerade as encode damage in the registered diagnostic; raw history/verdict remains stable | Establish production thresholds only from an owned/licensed natural-content corpus |
| P2 | Audio fingerprint semantics | **RESOLVED for algorithm/synthetic alignment (RFC #12):** legacy set Jaccard/paired Hamming remain unchanged; additive bounded offset/drift alignment rejects low coverage and records confidence | `core/qa/audio_fp.py::align_fingerprints`; adversarial/fixed-offset/drift tests; `specs/27-registered-qa-metrics-rfc.md` | Registered diagnostics resist short-overlap false confidence, but natural speech/music thresholds are not calibrated | Validate ranges and listening quality on the licensed corpus before any gating policy |
| P2 | SSCD extraction | **RESOLVED:** midpoint random-access used one FFmpeg process per frame | `core/qa/sscd.py::_extract_frames`; one-runner unit test; 32-frame real benchmark | 64 processes/default pair and cancellation latency removed | One multi-input runner process/file with single-threaded decoders; retain temporal alignment/coverage as separate QA work |
| P2 | Intel macOS ML supply chain | **LIMITED:** PyPI предоставляет только Torch 2.2.2 с 22 audit records | `pyproject.toml[ml]`, `docs/sscd.md`; local `pip-audit` | Риск при загрузке недоверенной модели; часть advisory не затрагивает используемые ops | Разрешён только официальный SSCD checkpoint с pinned SHA-256; для fully patched runtime использовать Apple Silicon/Linux/Windows |
| P2 | Calibration search | **RESOLVED for bounded synthetic objectives:** prefix-only first-success loop assumed monotonicity | `core/calibration/loop.py::calibrate`, `_next_factor`, `_rank`; monotone/non-monotone regressions | Biased or unnecessarily destructive tuned profile | Start/middle/end probe, baseline+bounds exploration, feasibility-first quality-preserving selection; natural corpus confidence remains NOT VERIFIED |
| P2 | Calibration clip/trial cache | **RESOLVED:** probe was existence-only and scores were not durable | `core/calibration/loop.py::_cut_test_clip`, `_load_trial`, `_save_trial`; replacement/corruption/reuse tests | Wrong source evaluated or expensive restart from zero | Content-keyed probe and atomic plan/metric/schema-keyed scored trials |
| P2 | Corpus identity/cache | **RESOLVED:** path-derived IDs changed on move and reused stale fingerprints after in-place replacement | `core/qa/corpus.py::Corpus.add`, `core/qa/corpus_db.py::CorpusDB.add_entry`; move/replacement/schema-upgrade regressions | Duplicate rows or false similarity diagnostics | Streamed SHA-256 identity; same-path stale row deletion in one transaction; schema-v2 content metadata |
| P2 | Transform compatibility | **RESOLVED for declared policy:** cross-domain restrictions were scattered and plugin incompatibilities were not evaluated | `core/transform_compatibility.py`, `core/preflight.py::_check_transform_compatibility`; graph/domain/order/pair regressions | Invalid HDR/audio/timeline/container requests could reach expensive encode | Central inventory plus existing source/encoder/container-aware preflight handlers; update graph and transform reference together |
| P2 | Calibration failures | **RESOLVED:** encode error increased factor | `core/calibration/loop.py::calibrate` | Infra failure became signal | Retry same factor, then abort |
| P2 | Aggressive profile | **PARTIAL:** destructive stack сохранён для compatibility, но описан как experimental и получает compound-quality warnings | `profiles/cid_aggressive.yaml`; `core/preflight.py::_check_quality_risks` | Audible/visual damage after another transcode | Keep opt-in; establish corpus-backed quality gates before any production recommendation |
| P2 | Geometric strength | **RESOLVED:** per-side crop doubled advertised maximum | `core/transforms/video_geom.py::_build_crop`; bounds test | Up to 18% axis loss | `max_strength` now total per-axis crop; documented migration |
| P2 | Aspect profiles | **RESOLVED (RFC #11):** custom fit-aspect profiles default to source-aware no-upscale; fixed-canvas shipped profiles preserve compatibility through explicit opt-in | `core/transforms/video_fit_aspect.py`; `core/preflight.py`; shipped profile contract snapshots and real platform-profile tests | Accidental enlargement no longer wastes encode/storage or invents detail | Keep `allow_upscale=true` explicit and surface preflight warnings for deliberate enlargement |
| P2 | Temporal cadence | **RESOLVED for software paths:** 1440 frames were treated as 60 seconds and high probabilities produced oversized expressions | `core/transforms/video_temporal_jitter.py`; real 24/30/60/VFR FFmpeg tests at allowed maxima | Frame-rate-dependent behavior and FFmpeg allocation failure removed | Bounded deterministic 24 Hz PTS-bucket permutation; Intel VideoToolbox base cadence is qualified, while transformed hardware cadence and NVENC/QSV/AMF remain NOT VERIFIED |
| P2 | Channel awareness | **RESOLVED for topology:** Haas rejects non-stereo; EQ/compand/smear/reverb/noise/pitch/resample preserve mono/stereo/5.1 channel count and 44.1/48/96 kHz inputs | `core/preflight.py::_check_audio_channel_layout`; `tests/integration/test_audio_layout_matrix.py` | Silent downmix/topology damage blocked for tested effects | Natural speech/music listening, phase correlation and clipping matrix remain quality qualification rather than topology correctness |
| P2 | YouTube settings | **PARTIAL:** layout-aware audio bitrate/48 kHz, explicit libx264 structure and locally qualified HEVC/AV1 random-access policy implemented; VideoToolbox H.264 adds explicit half-second IDRs | `core/pipeline.py::_main_audio_bitrate`, `_h264_upload_structure_args`, `_long_gop_args`; `tests/integration/test_encoder_bitstream_matrix.py`; final CI `33966344170`; hardware run `33966394736` | Codec defaults previously emitted inconsistent GOPs; Apple Silicon accepted `-g 12` but intermittently emitted a 21-frame IDR gap; libaom CQ+VBV failed and HEVC VideoToolbox could emit a contradictory Main/10-bit stream | Mono 128/stereo 384/surround 512 kbps; H.264 cadence now passes Intel and hosted Apple Silicon bitstream contracts; HEVC/AV1 use an internal closed/two-second random-access policy; qualify each NVENC/QSV/AMF device/driver |
| P3 | Scene segmentation | **RESOLVED:** scene gaps ignored target duration and edge cuts could create tiny segments | `core/segmenter.py::_plan_scene_segments`; static/sparse/edge regressions | One huge static segment or excessive micro-segments | Scene-preferred boundaries plus keyframe-safe target splitting and edge filtering |
| P3 | Encoder selection | **RESOLVED:** hardware-first default, silent override fallback and libaom discovery timeout false-negative | `core/encoder.py::pick_encoder`, `_probe_one`; policy/strict-override tests; Intel Mac libaom probe 15.63 s → 1.5–1.9 s | Lower quality/predictability or available AV1 encoder hidden | `quality` default plus `balanced|speed`; explicit override selected/rejected; probe-only libaom speed options do not alter production argv |
| P3 | AV1 Vulkan args | **RESOLVED for advertised surface:** CPU-frame graph cannot feed Vulkan hardware frames | `core/encoder.py::_CANDIDATES`; disabled-candidate regression | Runtime invalid command | Candidate disabled until init-device/hwupload path is end-to-end verified |
| P3 | Encoder cache | **RESOLVED locally:** key включает OS/machine/env/NVIDIA UUID+driver; a later segment encode failure invalidates the exact-job process cache | `core/encoder.py::_ffmpeg_version_hash`, `invalidate_encoder_capability`; segment runtime regression | Stale availability no longer skips the next exact capability probe after device-state changes | Retain discovery availability across transient session pressure; qualify driver-reset behavior on each hardware runner |
| P3 | Encoder cache routing | **RESOLVED:** in-flight detection re-read mutable cache path after parallel probes | `core/encoder.py::detect_encoders`; cache-path-switch regression | Background discovery could write into another run/test cache | Capture one cache path for load/probe/save transaction |
| P3 | Concat/sanitizer timeout | **RESOLVED:** fixed 3600-second subprocess paths bypassed shared runner | `core/segmenter.py::concat_segments`; `core/sanitizer.py::sanitize_bitstream` | Valid 3h+ job killed | Shared bounded-log/stall-watchdog runner; optional wall timeout via environment |
| P3 | Sanitizer format | **RESOLVED:** fixed MP4 temp and silent AV1→H264 conversion | `core/sanitizer.py` | Container mismatch/codec change | Target suffix/metadata preserved; any non-H264 contract rejected |
| P3 | Benchmark memory | **RESOLVED:** `RUSAGE_SELF` excluded FFmpeg and used platform-dependent units | `tools/benchmark.py::_ProcessTreeMemorySampler` | Misleading RAM metric | 100 ms aggregate process-tree RSS via psutil; labelled resource fallback |
| P3 | Seam tool | **RESOLVED:** output was compared to itself shifted by one second | `tools/seam_test.py`; unit + clean/defective real-FFmpeg regressions | Motion was mislabeled as a seam artifact | Source/output windows with reset PTS, bounded frame registration and previous-segment control; metric failure is fatal |
| P4 | Dual pipelines | **PARTIAL:** encoder policy унифицирован через `_encoder_args_for`; mapping/filter shapes ещё различаются | `core/pipeline.py` command builders | Remaining behavior/test drift | Продолжать shared argv helpers без второго pipeline |
| P4 | Web version | **RESOLVED:** hardcoded 0.9.0 | `web/app.py::create_app` | Incorrect diagnostics | Uses package `__version__` |
| P4 | Correlation/metrics | **RESOLVED:** events exposed only a run ID and web lifecycle transitions were not counted | `core/correlation.py`, `core/orchestrator.py`, `web/routes/run.py`, `web/metrics.py`; event-chain/lifecycle regressions and Docker smoke | Segment failure could not be traced to a plan/job; queue/resume health invisible | Deterministic run → plan → job → segment fields and bounded queued/active/resumed/terminal counters |
| P4 | Profile duplication | Platform YAML copies same values | `src/yt_uniquifier/profiles/*.yaml` | Magic-number drift | Generated inheritance/composition or validated shared fragments |
| P5 | Documentation | README chapters/subtitles and architecture claims stale | `README.md:35`, `CLAUDE.md:65` | Operator trusts behavior that fails | Update only after fixes; add verified capability matrix |

## Release blocker policy

Все P0 и P1 correctness items должны быть закрыты тестом, который сначала
воспроизводит defect на текущем коде. P2 quality items могут выпускаться позже только
при явной маркировке limitations. Любой `NOT VERIFIED` HDR/long-form/HW сценарий
остаётся release blocker для соответствующей заявленной capability, но не для
CLI-only SDR/x264 subset.
